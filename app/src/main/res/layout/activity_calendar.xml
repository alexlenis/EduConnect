<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- HEADER (MONTH + ARROWS) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="12dp">

        <ImageButton
            android:id="@+id/btnPrev"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_previous"/>

        <TextView
            android:id="@+id/tvMonth"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ιανουάριος 2026"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginHorizontal="16dp"/>

        <ImageButton
            android:id="@+id/btnNext"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_next"/>
    </LinearLayout>

    <!-- DAYS HEADER (PERFECT ALIGNMENT) -->
    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="7"
        android:layout_marginBottom="8dp">

        <TextView style="@style/CalendarDayHeader" android:text="Δ"/>
        <TextView style="@style/CalendarDayHeader" android:text="Τ"/>
        <TextView style="@style/CalendarDayHeader" android:text="Τ"/>
        <TextView style="@style/CalendarDayHeader" android:text="Π"/>
        <TextView style="@style/CalendarDayHeader" android:text="Π"/>
        <TextView style="@style/CalendarDayHeader" android:text="Σ"/>
        <TextView style="@style/CalendarDayHeader" android:text="Κ"/>
    </GridLayout>

    <!-- CALENDAR GRID -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/calendarRecycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
</LinearLayout>
